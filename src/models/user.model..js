class Brand {
   constructor(id, brandName, popylarLevel) {
      this.id = id;
      this.brandName = brandName;
      this.popylarLevel = popylarLevel;
   }
   getter() {
      return this;
   }
   setter() {}
}
